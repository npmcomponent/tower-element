var element = 'undefined' === typeof window
  ? require('..')
  : require('tower-element');

var assert = require('assert');

describe('element', function(){
  it('should define', function(){
    
  });
});